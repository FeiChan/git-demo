#include <stdio.h>

int main(void) {
	    int s, d;

	        // Keep reading pairs of integers until EOF
		while (~scanf("%d%d", &s, &d)) {
		printf("Read values: s = %d, d = %d\n", s, d);
		}
		
		printf("End of input detected.\n");
		return 0;
		}
		
	    
